## 2026-07-15 本地 Ollama 承担总结(零云端 token)

- 用户质疑总结是否消耗云端 token → 确认:此前总结是云端 agent 写的,每次定时都耗 token。
- 改为**本地 Ollama 生成总结**:本机已装 ollama 0.23.0,模型 `qwen2.5:7b`(Q4_K_M, 4.7GB, 中文能力OK),API `http://localhost:11434` 可达。
- 新增 `lib/llm.py`:标准库 urllib 调 `/api/chat`;`summarize_blogger`(英文推文→中文要点)、`summarize_market`(结构化行情→总评)、`is_enabled`(配置 enabled + 可达性探测)、`fill_llm_texts`(补齐缺失 summary/commentary,失败回退 None)。
  - **踩坑**:`urllib.request.Request` 构造不接受 `timeout` 参数(必须在 `urlopen` 传)→ 初版报 "Request.__init__() got an unexpected keyword argument 'timeout'"。已修。
  - **幻觉问题**:7B 模型在总评里把沪深300 +1.20% 误写成 -0.20%。收紧 prompt(只照抄给定数字、禁改正负号、temp 0.1)后准确。博主推文总结质量稳。
  - 耗时:两博主+总评约 93–140s(模型热身后降到 ~94s),9 点定时可接受。
- 接线:`daily_report.py` 在 `fetch_bundle` 与 `--from-cache` 两条路径都调 `llm.fill_llm_texts`;`render.py` 对 summary/commentary 做 `_esc` 转义 + `white-space:pre-wrap`(防止模型输出破坏 HTML)。
- `config.json` 加 `llm:{enabled:true, provider:ollama, base_url, model:qwen2.5:7b}`。
- 自动化 `automation-1784040166914` prompt 简化为单条 `./run.sh`(本地 Ollama 已承担总结,agent 不再手写)。Ollama 不可用时自动回退(博主展示原文、总评留空),不影响发送。
- 效果:每日定时**不再消耗云端生成 token**(仅 WorkBuddy agent 框架的固定开销)。若要彻底零云端 token,可改用系统 launchd/crontab 直接跑 `./run.sh`(脱离 WorkBuddy agent)。

## 00:28-00:37 迁移到系统级 launchd 定时(彻底零云端 token)
- 用户要求"变成系统级定时任务"。新建 `~/Library/LaunchAgents/com.dailyreport.market.plist`:
  Label=com.dailyreport.market; ProgramArguments=[/bin/bash, 项目/run.sh]; WorkingDirectory=项目绝对路径;
  StartCalendarInterval={Hour:9,Minute:0}(每天9点); RunAtLoad=false;
  StandardOut/ErrorPath=项目/logs/launchd.{out,err}.log; EnvironmentVariables.PATH 含 node bin。
- `run.sh` 已 chmod +x;Ollama 是 homebrew 常驻服务(homebrew.mxcl.ollama,PID 735),开机自启,9点调用必可用。
- 验证:用 `env -i PATH=/usr/bin:/bin:/usr/sbin:/sbin HOME=$HOME /bin/bash run.sh --no-send` 模拟 launchd 精简环境,完整跑通(期货+新浪兜底板块+推文+本地Ollama总结+渲染),不缺依赖、不出错。数据日期自动标"前一交易日"(7/15凌晨跑→最新交易日7/14,符合"前一天总结")。
- `launchctl load` 成功,`launchctl list` 显示 com.dailyreport.market(- 0,已加载待触发)。
- **停用 WorkBuddy 自动化** automation-1784040166914(status=PAUSED),避免与系统任务双发。
- 当前状态:系统 launchd 每天9:00 独立发报告,全程本地、零云端 token。日志在 logs/launchd.out.log。
- 运维命令: `launchctl start com.dailyreport.market`(手动立即跑一次,会发信); `launchctl unload ~/Library/LaunchAgents/com.dailyreport.market.plist`(停用); 日志 `tail -f logs/launchd.out.log`。

## 00:36 手动触发 launchd 任务验收(成功)
- `launchctl start com.dailyreport.market` 手动跑一次,完整链路跑通并**成功发信**:
  抓取期货(00:36:58,55品种) → 行业板新浪兜底成功/概念板东财限流失败(本机IP,环境性) → 博主推文(aleabitoreddit 5条+Ariston 1条) →
  本地 Ollama 生成两博主总结+总评(00:41:27-00:42:17,约93s) → 渲染HTML → 发信(00:42:20,发往 irene06@qq.com + 3101928126@qq.com,【成功】)。
- **确认全程零云端 token**,系统任务已具备无人值守能力。
- 排查注记:launchd 任务的 Python logging 默认写 stderr → 落 `logs/launchd.err.log`,stdout(out.log)为空属正常,别误判为没输出。
- 概念板块东财限流是持续性环境问题(非代码 bug),明早9点若东财恢复即自动补齐;行业板有新浪兜底不空。

## 01:05-01:18 新增「美股市场」模块(⑤)
- 用户要求加美股:各板块涨跌 + 微软/英伟达/阿里 涨跌 + 三只当天新闻。
- 新文件 `lib/us_stocks.py`:
  - **板块**:11 个 GICS 行业 SPDR ETF(XLK 科技/XLE 能源/XLF 金融…)作涨跌代理,按涨跌幅排名。
  - **个股**:MSFT/NVDA/BABA 涨跌+市值,来自 Yahoo 批量 quote 接口(`/v7/finance/quote?symbols=`,单次请求全拿)→ query1 失败回退 query2,遇 429 退避重试。
  - **新闻**:改用 **Google News RSS**(`news.google.com/rss/search?q=<公司>`),免费无鉴权、聚合广;原 Yahoo RSS 在测试中被 429 且 Google News 更稳。**新闻独立于行情**:行情缺失时价格显示"-",新闻照常抓。
  - 交易日按 Yahoo `regularMarketTime` 转 ET;新闻过滤到该交易日(含盘后)。
- 接线:`daily_report.py` 调 `get_us_stocks` 写入 `bundle["us_stocks"]`;`render.py` 新增 ⑤ 美股市场章节(板块表/个股表/各股新闻列表,涨跌红绿,新闻标题可点击外链);`llm.fill_llm_texts` 新增本地 Ollama 美股概览 `us_summary`(数字严格照抄,防幻觉);`config.json` 加 `us_stocks.tracked`(含 `news_query`,可增删标的)。
- **踩坑**:本机 IP 凌晨测试 Yahoo 频发请求被临时 429 冷却(>5min,双主机均限)。生产 9 点运行早已冷却恢复,正常;且批量接口每天仅 1 次请求不会触发。stooq 已被反爬挡(返回 JS 验证页),不可用。
- 验证:用实时新闻 + 此前成功抓取的真实行情价合成 bundle 渲染预览(`reports/preview_us_20260715.html`),本地 Ollama 概览数字准确。正式运行待 Yahoo 冷却后自动补全价格。

## 01:32 美股模块完整验收 + 修幻觉 bug
- `launchctl start` 触发整链路跑通并**成功发信**(01:38:28 两邮箱),验证美股模块已接入自动流程。
- **暴露 bug**:本轮 Yahoo 仍处 429 冷却(行情全空),`summarize_us` 在无涨跌数字时仍令 Ollama 幻觉出涨跌判断("微软英伟达领涨"/"BABA 表现出色"),且输出为英文。
- **修复** `lib/llm.py`:
  1) 行情缺失(`quotes_ok=False`)时**不再调模型**,改 `_us_news_fallback` 规则模板直接罗列三只个股当日新闻标题(零幻觉、零本地算力、零英文输出风险);
  2) 行情正常分支 system prompt 补"请使用中文",并保留数字照抄约束。
- 重测:修复后 fallback 耗时 0.0s、纯中文框架、无模型臆测;新闻标题为 Google News 英文原文(客观转录,非幻觉)。重新渲染 `reports/preview_us_fixed_20260715.html`。
- 已知体验项:Google News 标题为英文(行情正常时 Ollama 概览会中文转述;行情缺失 fallback 列原文)。可选增强:行情正常时也把新闻要点中文化。待用户决定。
- 注:邮箱里 01:38 那封的美股概览为修复前幻觉版,不可靠;明早9点定时(届时 Yahoo 恢复)将正常。

## 22:35-23:0 用户四项改动(均已落地验证)
- **删除持仓量排行板块**:render.py 删"持仓量排行" h3+表;futures.py 移除 by_hold 排序与结果项(其他表仍保留持仓量作上下文列)。
- **缠论改标准实现(非简化)**:`lib/chan.py` 重写。流程=K线包含合并→严格顶/底分型→笔(贪心极值+笔破坏)→中枢(三笔重叠)→MACD红绿柱面积背驰→一买/二买/一卖/二卖。返回 signal_price/止损参考位/bias(次日倾向)/fresh。
  - **关键修复用户痛点(苹果=已过去的机会)**:新增 `_freshness()`——信号成立后比较最新收盘价,若已回落(卖点已兑现)则标记 fresh=False 并追加"该卖点已兑现,当前不宜追空,宜观望或等待二买";只在价格仍处信号侧时标 fresh=True。日线收盘信号天然滞后,改为以"次日倾向+止损参考"指导操作。
  - render.py 缠论表加"倾向"列(偏多绿/偏空红/观望灰),标题改"标准缠论",脚注"标准算法"。
- **博主只留观点总结(含态度)**:render.py `blogger_section` 删除原文展示(只留 summary 卡片,无原文时提示"原文不展示");`llm.summarize_blogger` 改写 prompt——先总体立场/情绪,再分主题要点且每条标注「态度:看多/看空/观望/中性」与依据,num_predict 600→800。
- **美股行情重试爬取**:`us_stocks.py` 增强——`_fetch_yahoo_quotes` 在 query1/query2 的 /v7/finance/quote 均失败后,兜底用逐标的 /v8/finance/chart;`get_us_stocks` 在首次为 None 时 sleep 45s 后重试一次(应对 Yahoo 429 临时限流)。
- 端到端 `./run.sh --no-send` 验证通过(22:53 渲染成功):期货走新缠论、东财板块本次恢复(行业+概念均成功)、博主总结含态度、美股新闻正常。
- **环境注记**:本会话为验证反复压测 Yahoo,Yahoo 对本机 IP 持续 429 限流(150s+45s 重试仍全失败);运行期 45s 重试窗口曾短暂恢复(微软价已入 bundle)。生产 9 点每天仅 1 次请求不会触发,届时美股行情完整。

## 23:09-23:25 用户要求实跑一版(--no-send 预览)
- 用户:「你现在生成一版给我」。跑 `./run.sh --no-send`,23:25 完成,耗时 7m27s。报告 `reports/daily_report_20260715.html`。
- 正常:期货+标准缠论全跑通(苹果=一卖顶背驰,已标 fresh=False/不宜追空/给次日倾向+止损位);板块东财再被限流→新浪兜底成功(行业84板块);博主 @aleabitoreddit 命中 3 条→Ollama 生成带态度总结,@Ariston_Macro 当日 0 条推文(栏显无更新);本地 Ollama 总评/美股概览均生成;美股新闻正常。
- **美股行情仍全缺**:Yahoo 对本机 IP 持续 429(首次 /v7 + 45s 重试 + /v8 chart 兜底全败),价格栏显 `-`,走规则模板 fallback 概览(零幻觉)。限流自 22:53 起未消退(本会话反复压测副作用),非代码问题。
- 用户尚未确认是否要补独立美股数据源(akshare/新浪美股)重跑完整版。下一步待定。

## 23:48 新闻中文总结 可行性调研(用户:抓取的新闻每次"浏览并中文总结")
- 实测数据源(结论:主流聚合 RSS 链接都被混淆成跳转页,自动流程拿不到新闻正文原文):
  - Google News RSS: description 仅为"标题+媒体名"聚合重复,无正文;link=news.google.com/rss/articles/<id> 聚合页,直连返回 400,base64 解码 id 失败(Google 2024 后改编码),拿不到真实文章 URL。
  - Bing News RSS: link=apiclick.aspx 跳转页,urllib 跟随重定向仍返回自身(拿不到真实 URL);中文关键词"英伟达/微软 股票"返回 0 条,"阿里巴巴 股票"仅 2 条(描述含中文导语但噪声大);英文 ticker NVDA/MSFT/BABA 各 11 条但噪声极大(日文/无关综艺 BABA)。
- 落地路径(待用户定):
  - 路径一(立即可用,零成本):基于每条新闻"英文标题+媒体来源",本地 Ollama 生成中文翻译+一句背景点评(约束:只据标题,不确定直译不编造)。解决看不懂英文,但非读正文。
  - 路径二(读摘要级,需用户注册 finnhub.io 免费 key):Finnhub company-news 返回 headline+summary(文章摘要)+真实 url,本地 Ollama 把 summary 中文总结。质量更高、Finnhub 免费额度独立于 Yahoo、不耗 token。
  - 推荐:先路径一立即出效果;要高一级"读摘要"则给 Finnhub key 升路径二(路径一可保留作兜底)。
