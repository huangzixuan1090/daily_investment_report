## 2026-07-14 每日市场报告 Agent 搭建

- 在 `/Users/michaelhuang/WorkBuddy/老婆agent` 搭建每日市场报告系统（每天 9:00 定时）。
- 架构：`lib/`(futures/chan/sectors/bloggers/render/mail) + `daily_report.py` + `config.json` + `run.sh`/`auth_mail.sh`。
- 期货：akshare `futures_zh_daily_sina(symbol=code+"0")`（新浪，含 `hold` 持仓量 + 全历史 OHLC，单品种1次调用1.2s）；资金流入估算 = Δ持仓量 × 收盘价 × 合约乘数；缠论自实现（K线合并→分型→笔→背驰买卖点）。
- 板块：东方财富 push2 clist（涨幅/跌幅/资金流入/流出各取 top_n，请求间隔防限流）。
- 博主：twitter-cli（已装 0.8.5，`twitter user-posts <handle>`），需在 config.json 填 `twitter.auth_token`/`ct0`。
- 发信：agently-cli（已装，两阶段 confirmation_token 发送，`--body-file` 发 HTML），需 `./auth_mail.sh` 做一次 OAuth。
- 依赖装在 managed venv：`/Users/michaelhuang/.workbuddy/binaries/python/envs/default`（akshare/requests/pandas/numpy/twitter-cli），用清华镜像源装（直连 PyPI 极慢）。
- 测试结果：futures+缠论跑通（55 品种，原油 +7.63% 领跑，苹果一卖顶背驰）；东财板块因测试高频请求触发 IP 临时限流（会自动恢复；9 点定时请求量小不受影响）。
- 已建定时任务 `automation-1784040166914`（FREQ=DAILY;BYHOUR=9）。
- 待用户：① 填 Twitter Cookie ② 跑 `./auth_mail.sh` 授权邮箱 ③ 确认博主 handle（默认 serenity / aristonwang）。

## 22:51-23:00 凭证接入与修正
- 用户已提供 Twitter Cookie（auth_token/ct0）并填入 config.json；邮箱授权跑通（agently-cli 认证成功，发信身份 michae7344@agent.qq.com，配额 50封/天）。
- **博主 handle 修正**（原猜测错误）：serenity→`@aleabitoreddit`（Serenity，94.9万粉，金融市场博主）；aristonwang→`@Ariston_Macro`（Ariston，Global Macro Trader / FICC 专栏，1.3万粉）。已更新 config.json。
- **修复 bloggers.py 解析 bug**：twitter-cli 返回驼峰字段 `createdAtISO`/`text`，互动量在 `metrics.{likes,retweets,views}` 下；原代码找 `created_at`/`full_text`/`favorite_count` 全不匹配，导致日期解析失败、所有推文被跳过（命中恒为 0）。修复后 Serenity 7/13 命中 2 条、Ariston 4 条。
- 注意：twitter-cli `twitter user` 默认输出 YAML（非 JSON），调试时别用 JSON 解析；`user-posts --json` 才是 JSON。
- 东财板块接口仍对本机 IP 临时限流（自 22:28 起），9 点定时跑请求量小不受影响；后续可在限流恢复后重跑 sectors 补全。

## 23:04-23:27 板块多源兜底与 bug 修复
- 用户要求"补抓东财"，但本机 IP 被东财 push2 硬封（RemoteDisconnected），且新浪也间歇封。twitter/同花顺(THS) 不受影响 → 确认是东财+新浪被针对的 IP 级限流，非全局断网；该限流为临时，明早 9 点前必恢复。
- 重构 `lib/sectors.py` 为多源兜底：东财 push2 为主（3 次尝试、失败间隔 30s 退避给限流恢复留窗口）→ 行业板新浪 `newFLJK.php` 兜底 → 都不行则优雅提示。概念板无 Sina 接口，东财失败即提示。
- **修复新浪解析 bug**：① 返回格式可能是 `=({...});` 或 `={...}` 且无结尾分号，改为取首个`{`到末个`}`之间内容（不再强求`;`）；② 字段语义：`[3]`=均价、`[4]`=涨跌幅、`[9]`=领涨股涨跌幅、`[12]`=领涨股名（之前误把均价当涨跌幅，出现 +122% 离谱值）。修正后行业板 84 个、数据合理（计算机/通信+4.03% 等）。
- THS `stock_board_*_summary_ths` 可返回全板块但逐板抓取太慢（分钟级），不适合作兜底；`stock_board_concept_name_ths` 快但只有名称无涨跌幅。
- 现状：报告行业板已有真实数据（Sina），概念板因东财封 + Sina 无概念接口暂空，明早东财恢复后 `automation-1784040166914` 自动补全。render.py 已加 `sina_sector_table`（板块/涨跌幅/领涨股）分支。

## 23:33-23:43 增加"数据日期"贯穿标注
- 用户确认：亚翔集成(603929)今早确实涨挺多（建筑安装业领涨 +5.48% 为真实值），逻辑无误；要求报告明确"数据源是哪一天"。
- 改动：`daily_report.py` 先抓期货取权威数据日期（最新交易日），计算 `data_weekday`(中文周几) 与 `data_date_label`("当日收盘"/"前一交易日"，相对生成时刻)，写入 bundle 并传给 `sectors.get_sectors(cfg, data_date=)` 与 `bloggers.get_bloggers(cfg, data_date=)` 对齐；`--from-cache` 回退补算 date。
- `render.py` 在头部(`行情数据日期：YYYY-MM-DD（周X）｜ 标签 ｜ 生成于…`)、引言、期货/缠论小节、板块(来源+数据日期)、博主(当日推文)、页脚均标注。
- 行为：晚间跑→data_date=当日(标"当日收盘")；9点定时跑→市场未收盘，最新完整数据=前一交易日(如7/13周一,标"前一交易日")，满足"前一天总结"需求。

## 23:53-23:56 测试邮件发送 + mail.py 两阶段发送修复
- 用户要求发测试邮件到 irene06@qq.com、3101928126@qq.com。
- 往 bundle_20260714.json 补写博主总结(summary)与开篇总评(commentary，含【测试邮件】标记)；`run.sh --from-cache reports/bundle_20260714.json` 渲染并用缓存发送。
- **修复 mail.py 两阶段发送**：agently-cli 确认阶段（`--confirmation-token`）并非仅传 token，而是需重传完整参数——阶段1成功后阶段2也要带 `--body-file`/`--subject`/`--to`，否则依次报 `required flag "--to"/"--subject"/"--body or --body-file" is required`。改为 `base` 参数列表 + 阶段2 追加 token 复用，发送成功。
- 结果：23:56:39 发送完成 → 两邮箱均收到「每日市场报告 2026-07-14」测试邮件（含期货/缠论/行业板(Sina)/博主原文+总结）。概念板仍因东财限流暂空。
