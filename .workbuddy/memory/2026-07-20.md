# 2026-07-20 工作日志

## 完成：基于 PDF 意见(v3 修改)的两项改造
用户 PDF `/Users/michaelhuang/Desktop/v3 修改(1).pdf` 两条意见：
1. **缠论改为 30 分钟级别 + 只写当天分析**
   - `lib/futures.py` 新增 `fetch_main_minute(code, period="30", bars=40)`，取 30分钟K线(字段 datetime/open/high/low/close/volume/hold)，最新到当天。
   - `get_futures_overview` 循环内改 append 30分钟K线版本（删旧日线分支），带 `last_date`。
   - `lib/llm.py` `analyze_chan_with_llm` 重写：并发分析**全部**品种（去掉"只筛16个"），system prompt 强约束"仅基于【当天】30分钟K线，严禁引用当天之前的旧日期(如6月8日)"；`ThreadPoolExecutor` 并发(`chan_concurrency`)。
   - config.json 加 `chan_timeframe:"30"`、`chan_bars:40`、`chan_concurrency`(6→2，见下)。
2. **新增公众号文章总结监控**
   - 新建 `lib/wechat.py`：Sogou 微信搜索 type=2 解析 `txt-box`（标题/来源/时间戳/摘要）；type=1 本环境返回空卡片故弃用；来源匹配+近期过滤(recent_days=120)+indirect 标记（未能精确定位账号时提示）；直链被反爬拦截不抓正文，仅用标题+摘要。
   - config.json 加 `wechat.accounts:["地平线全球策略","行知交易员"]`、`max_articles:6`。
   - `lib/llm.py` 新增 `summarize_wechat`（Ollama 基于标题+摘要生成中文观点总结）；`fill_llm_texts` 遍历 wechat.accounts 补齐 summary。
   - `daily_report.py` `fetch_bundle` 在博主后、持仓前加 wechat section。
   - `lib/render.py` 新增 `wechat_section`，章节重排：⑦ 公众号观点监控、⑧ 美股板块涨跌分析、⑨ 我的持仓（原持仓 ⑧→⑨，已修正编号碰撞 bug）。

## 修复
- render.py `holdings_section` 标题原为 `⑧ 我的持仓`，与 `⑧ 美股` 碰撞 → 改为 `⑨`（3 处 + docstring）。
- Ollama 超时：`chan_concurrency` 6→2（qwen2.5:7b 并发>2 易超 180s 超时），单调用时反而更短。

## 验证
- 编译通过(py_compile)，deps OK(akshare/requests/pandas/numpy)。
- 公众号复测：行知交易员 indirect=False(4篇,近期)；地平线全球策略 indirect=True(3篇,已过滤2017/2021旧文,保留2026近期)。
- 全量重跑 `--no-send --reuse-cache` 完成 → reports/daily_report_20260720.html：
  - 缠论 55/55 全分析(concurrency=2 无超时,约 23 分钟)，2 个机会：乙二醇(eg)一买@4668(21:30底背驰)、纯碱(sa)一卖@1007(14:15顶背驰)——**理由均为当天盘中时间，无旧日期，PDF 意见1 达成** ✓
  - 公众号：地平线全球策略 indirect=True 3篇+总结、行知交易员 indirect=False 4篇+总结——**PDF 意见2 达成** ✓
  - 章节 ①…⑨ 连续，持仓修正为 ⑨ ✓
  - 降级(非本次改动引入)：美股 Yahoo 403 限流→quotes_ok=False/0 板块(转瞬限流)；持仓失败。

## 又一 bug 修复：daily_report.py `_fetch_holdings` 中 `script` 未定义
- 现象日志：`持仓采集失败: name 'script' is not defined`。无当日 holdings 缓存且走 TWS 分支时必崩，会影响 8:00 定时任务。
- 修复：新增 `script = common.ROOT / ibkr.get("script", "ibkr_collect.py")` 并加存在性检查。config 可用 `ibkr.script` 覆盖。已 py_compile 通过。
- 注：持仓真正跑通仍需本机 TWS 实盘(7496)+API 启用；本次报告为修复前生成，故持仓段仍显示旧报错。
