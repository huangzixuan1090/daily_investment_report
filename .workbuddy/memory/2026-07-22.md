# 2026-07-22 工作日志

## 修复：今日(7/22)报告未发送 + 缠论无机会
- **渲染崩溃根因**：`lib/render.py` 渲染「③ 股市板块表现」的东方财富板块时调用了未定义函数 `sector_table` → 全部数据抓完却在渲染阶段崩溃，HTML 没生成、邮件没发。已补 `sector_table()`（板块/涨跌幅/主力净流入/净流入占比/成交额/振幅）。
- **缠论机会为 0 根因**：本地 qwen2.5:7b 对多级别联立买卖点判定极保守且偶发超时，对全部品种返回"无信号"。已加**确定性规则缠论兜底**（`lib/chan.py` 笔→中枢→MACD背驰），逻辑：优先 LLM 当日信号，模型没给则用规则引擎补。验证可稳定产出当天信号（白银/沪深300 一卖）。
- 重新生成 `cache/futures_20260722.json`（规则兜底，llm.enabled=False 跳过慢速 LLM），从全部 7/22 缓存 + 实时 global_markets 重新渲染 `reports/daily_report_20260722.html`（--no-send，待用户确认后发送）。

## 用户参考样式：资金异动四象限卡片
- 用户给 `~/Desktop/fund_flow_20260721.html` 作参考。已将 **① 期货市场** 和 **③ ETF** 的「涨跌幅+资金流入/流出」改为同款「资金异动四象限」卡片（涨/跌 × 资金流入/流出），含汇总条 + 自动解读。
- 期货行新增 `oi_change`/`oi_change_pct`（增减仓手数/百分比）供卡片展示。

## 机型/本地模型结论（重要，纠正旧认知）
- **Ollama 实测 100% GPU（Metal）**，并非 CPU-only —— M3 加速生效。慢的真因是 7b 模型能力弱 + 早期把 5 周期 K 线全塞一个 prompt 导致超时。
- **M3 16GB 上限 = 14B Q4（~9GB）**；30B+ Q4（~18GB+）塞不进统一内存会 swap 卡死。推荐 qwen2.5:14b / qwen3:14b / deepseek-r1:14b（推理型但慢）。模型名由 `config.llm.model` 配置，无需改代码。
- 因已有规则缠论兜底，LLM 定位为"增强层"；换 14B 仅提升质量，不解决 55 只全跑耗时问题（14B 单只约 45-60s）。

## 14B 落地失败 + 回退 7B（关键环境教训）
- 用户选"用 14B"。已 `ollama pull qwen2.5:14b` 成功（9.3GB），`config.llm.model`→14b、`chan_concurrency`→1（避免双并发≈18GB超内存）。
- **14B 在 5 周期全量 K 线超大 prompt 下必超时**（单只 2-3 分钟 > 240s），全量 55 只不可行。改为**轻量补写**：只把规则引擎已算出的结论喂 14B 写解读（不传 K 线），新增 `lib/llm.interpret_chan_signal`（`_chat` 加 `timeout` 参数，默认 90s）。
- **致命环境坑（务必记住）**：本机 M3 16GB 在后台常开 Kimi.app / Doubao / Edge / WorkBuddy 多窗口时，**统一内存爆**（vm_stat：wired≈11GB + compressor 存储≈12GB），导致 ollama 推理卡死——实测 14B 短 prompt 60s 超时，**连 7b 都 30s 超时**。根因不是模型大小，是内存被多 app 吃光触发 swap/压缩。
- 多次拉起/杀掉 14B 测试（每次加载 9.3GB 模型，wired 内存没及时回收）+ 常开 app 累积导致爆内存。
- **决策**：用户选"先回退 7b 出报告"。已 `config.llm.model`→`qwen2.5:7b`、`chan_concurrency`→2。但 ollama 内存仍爆，故渲染时**临时 `llm.enabled=False`** 跳过博主/总评生成（fill_llm_texts 内 summarize_* 在 enabled=False 时 return None 快速跳过），复用全部 7/22 缓存 + 规则引擎缠论信号，11s 渲染出 `reports/daily_report_20260722.html`（缠论 4 机会：乙二醇/LPG/白糖/沪深300 一卖，rule 引擎出；博主/总评降级为空）。
- **生产环境待办**：用户释放内存（关 Kimi/Doubao/多余浏览器标签 或 重启 Mac）后，重跑 `./run.sh`（ollama 可用时）即带 7b 完整版（博主/总评由 7b 生成）。14B 留待内存充裕时再启用。
