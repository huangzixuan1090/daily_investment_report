## 00:01 IBKR 实盘连接 + 持仓股新闻 可行性测试

- 用户要求:研究 trading_bot 项目,测试能否通过 IBKR API 连实盘拿持仓 + 持仓股新闻。
- 环境:trading_bot 有独立 `.venv`(装 ib_insync==0.9.86);本机 **TWS 实盘端口 7496 OPEN**(7497/4001/4002 均 closed)→ 用户跑的是实盘账户。
- 纯只读测试(`.venv/bin/python` 跑):`IBKRTrader(port=7496, client_id=99).connect() → get_account_summary() → get_positions() → disconnect()`,**全程只读,未调用 reqMktData/reqMarketDataType/placeOrder**。
  - ✅ 连接成功,账户 `U19891803`
  - ✅ 账户: NetLiquidation 44,252.72 / UnrealizedPnL -535.55 / BuyingPower 134,597.86 USD
  - ✅ 实时持仓(**与 strategy.json 缓存的 APP/DASH/RDDT/HUBS/NBIX 完全不同,说明已换仓**): `9606`(港股 Duality Bio, 9606.HK)100股@223.02 / `NVDA` 10股@215.97 / `MSFT` 46股@392.28 / `BABA` 40股@136.00
- 良性警告:`cq`/`b0` Error 321 = ib_insync `connect()` 内部自动"账户更新订阅",TWS 只读模式拒绝,但持仓/账户数据已正常返回,**非主动写入**。用户初测监测到的"写入请求"来自第一版脚本的 `reqMktData`(news tick),已删除。
- 新闻:IBKR 原生 news tick(`reqMktData` 292)需 TWS 订阅新闻权限,未走通;外部源 **Google News 按持仓 symbol 抓标题已验证可用**(示例:MSFT 微软裁员4800人、BABA 阿里+苹果 Qwen AI 集成、NVDA 中国3企获美批准采购、9606 Duality Bio 年报)。
  - ⚠️ `.venv` 缺系统根证书 → urllib 访问 https 报 `SSL CERTIFICATE_VERIFY_FAILED`;测试用 `ssl._create_default_https_context = ssl._create_unverified_context` 绕过(仅公开 RSS)。正式方案应装 certifi 或跑 `Install Certificates.command`。
- 结论:**完全可行**。建议把"IBKR 实时持仓 → 外部新闻(Google News) → Ollama 中文总结"接入每日报告,替代原固定 MSFT/NVDA/BABA,实现"我的持仓个性化新闻"。
- 安全红线:连实盘只做只读查询;下单接口(`buy_stock`/`sell_stock`/`placeOrder`)绝对不调用。持仓含港股(9606.HK),接入报告时需按市场区分行情/新闻源。

## 00:17 用 IBKR 法生成「昨收持仓 + 新闻」报告一版

- 新建 `gen_holdings_report.py`(独立可跑,纯只读):连 TWS 7496 拉 `ib.positions()` + 账户 → 按持仓 symbol 抓 Google News RSS → 本地 Ollama qwen2.5:7b 逐只生成中文总结(利好/利空/中性+理由) → 渲染 HTML。
- 跑通:`./code/trading_bot/.venv/bin/python gen_holdings_report.py`(总耗时 ~5min,主要是本地模型逐只总结慢),输出 `reports/holdings_report_2026-07-15.html`。
- 结果:持仓 4 只(9606/NVDA/MSFT/BABA)均含账户概览+持仓表+逐只中文总结+新闻原文链接。NVDA/MSFT/BABA 新闻偏 7/13-7/15 实时;BABA 利好(获准为中国苹果设备供 Qwen AI)、NVDA 利好(中企获美批准购芯)+利空(机构减仓)。
- 渲染只展示「成本金额」(IBKR 只读不提供实时市值,需 market data 订阅);未启用行情订阅。
- 待办:用户下一步可能要把此模块并入每日市场报告(替代原固定美股 MSFT/NVDA/BABA),需处理:① 定时任务需 TWS 常开 ② 港股代码/新闻源区分 ③ 证书修复(`.venv` 装 certifi)。

## 00:30+ 4 项改动落实(删账户概览 / 行情重试 / 昨日新闻 / 并入每日报告)

- 用户 4 要求:① 删账户概览板块 ② 记得有行情订阅,重试 ③ 新闻只抓昨天,无则不要 ④ 并入每日市场报告。
- 新建 `ibkr_collect.py`(独立可跑,纯只读;跑在 trading_bot/.venv):`collect(cfg, data_date)` → 连 TWS 7496 拉 `positions()` → 行情快照 `get_market_data` 依次 mdt=1/3/4 重试(取昨收价)→ 失败落 Yahoo 兜底 `get_external_close`(港股加 .HK)→ 按持仓 symbol 抓 Google News → `news_on_date` 按 **Asia/Shanghai 日期==data_date** 过滤昨日新闻 → 有新闻才调本地 Ollama 逐只中文总结 → 渲染独立 HTML / 输出 JSON。
- `daily_report.py` 已集成:fetch_bundle 里用 `subprocess` 调 `ibkr_collect.py`(读 config.ibkr.venv_python / enabled / port / client_id / holdings_meta),产物 `reports/holdings_YYYYMMDD.json` 并入 bundle,键 `ibkr_holdings`;`render.py` 新增 `holdings_section()` 替换原 `us_stocks` 板块(标题「⑤ 我的持仓(IBKR 实盘)」),删账户概览、删 us_stocks 调用、更新头注/脚注。
- `gen_holdings_report.py` 改为薄包装:调 `daily_report.generate_bundle` 后取 `bundle['ibkr_holdings']` 渲染独立 HTML(便于快速预览)。
- **关键澄清**:IBKR 行情请求报 `10089`/`10168`(需订阅)实为**警告**,IBKR 仍返回**延迟行情价格**(NVDA 211.8 / MSFT 384.93 / BABA 112.32 / 9606 235.4 均为合理 7/15 收盘)→ `price_src="ibkr(mdt=3)"` 标注正确,用户"有行情订阅"在延迟层面生效。`get_market_data` 回退条件已加 `and close>0` 防 `marketPrice()` 失败返 0 误判。
- 新闻过滤经验证正确:所有保留项 pub 换算 Asia/Shanghai 均为 data_date(7/15)当天。新增 `clean_news` 去 `META_TITLE_QUOTE` 等 Google News 占位标题 + 去重 + 去「- 媒体名」尾巴。
- TWS 不可用时 `ibkr_collect` 异常 → daily_report 捕获跳过该板块,不影响其余模块。

## 01:01+ 改进持仓股中文总结 prompt(更详细/有分析深度)

- 用户反馈:NVDA/MSFT 总结太笼统(利好/利空/中性+套话),要求写详细、写进本地模型 prompt。
- 改动 `ibkr_collect.py`:
  - 新增 `build_summary_prompt()`:要求**结构化深度分析**——逐条【事实概述 / 影响逻辑(传导路径) / 倾向+置信度(高/中/低)】+【综合研判:整体情绪/关键催化剂/关键风险/对持仓者启示】;严格只据新闻、区分事实与「分析推断」、禁空话堆砌。
  - `collect()` 调用改为 `build_summary_prompt(...)` + `num_predict=1100`(原 600,太短导致收尾笼统);新闻上下文 `desc[:400]`(原 200)。
  - 新增 `--rerun-summary` 模式:读现有 `--out` JSON,仅用新 prompt 重跑 summary 并重渲染 HTML,**不连 IBKR**,快速验证效果。
- 集成 `daily_report.py` 调 `ibkr_collect.py` 整体(collect)已自动用新 prompt;render 无需改。

## 01:16 定时任务改到 08:00

- 用户要求把定时任务从 9:00 改到 8:00。
- 系统 launchd plist `~/Library/LaunchAgents/com.dailyreport.market.plist` 的 `StartCalendarInterval.Hour` 由 9 改为 8;`plutil -lint` 通过;`launchctl unload`+`load` 重新加载生效,`launchctl list` 确认已注册,PlistBuddy 读取 Hour=8 / Minute=0。
- 顺手把 WorkBuddy automation `automation-1784040166914`(原「每日市场报告(9点)」,PAUSED) 同步改 rrule 为 `FREQ=DAILY;BYHOUR=8;BYMINUTE=0;BYSECOND=0` 并改名「每日市场报告(8点)」(仍 PAUSED,不运行)。
- 主定时任务:`com.dailyreport.market` 每天 08:00 跑 `run.sh`;依赖 TWS 实盘常开(7496)+Ollama 运行,否则持仓板块跳过/总结兜底。

## 21:5x 新增「ETF 表现」板块（涨跌幅 + 资金流入/流出），插在行业板块之后

- 用户要求:在「③ 股市板块表现」的**行业板块之后**加 ETF 表现（涨跌幅 + 主力资金流入/流出）。
- 新增 `lib/etf.py`:东财 ETF 排行接口,`fs=b:MK0021,b:MK0022`(沪深全市场ETF)。字段 f3=涨跌幅/f62=主力净流入/f184=净流入占比/f6=成交额。取涨跌幅 top + 主力净流入/净流出 top（各 top_n=15）。输出 `etf_{tag}.json` 缓存。
- 接入:`config.json` 加 `etf.top_n=15`;`daily_report.py` 在板块后 `bundle["etf"]=get_section("etf",...)`;`render.py` 新增 `etf_table()`+`etf_section()`(h3 标题 + 4 张子表:涨幅前/跌幅前/主力净流入前/主力净流出前,加 h4 样式),并在板块 for 循环内 `if b.get("key")=="industry": parts.append(etf_section(etf))` 实现「行业板后紧跟 ETF」。
- **踩坑/修复**:`push2.eastmoney.com` 对本机 IP 持续限流(`Connection aborted`),`_probe` 直连成功但 `http_json`/`requests` 重试也失败。改为 `HOSTS=[push2, push2delay]` 双主机兜底 —— `push2delay.eastmoney.com`（延时行情主机）验证可用(total=1292,字段一致),限流时自动落延时源。
- 待优化:当前 ETF 含货币/债券型(如「银华日利ETF」涨0.0 净流入居首),后续可按名称过滤只留股票型 ETF;数据源日期与板块同口径(抓取最新交易日,标注报告统一 data_date)。

## 持有缓存复用 (22:0x 补充)
- `_fetch_holdings(cfg, data_date, reuse_cache)` 增加 reuse_cache 分支:`--reuse-cache` 且 `cache/holdings_{YYYYMMDD}.json` 已存在且 ok,则直接读缓存跳过 IBKR 实时连接(更快,也便于 TWS 关闭时兜底)。`fetch_bundle` 已传 reuse_cache。
- 实测:`./run.sh --no-send --reuse-cache` 复用全部模块缓存(含 holdings)可秒级出图,不再卡在 IBKR 行情重试(3种mdt类型×4标的×重试极慢)。
- 验证:`daily_report_20260716.html` 含 ETF 板块(行业板块后插入),顺序=板块(2190)<ETF(13865)<持仓(22881),数据正常。
